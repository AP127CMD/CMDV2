name: Refresh data snapshots

# V2 is a mirror: it pulls Command Center's published flight-data.js and the
# ap127-data-api progress worker, then commits any change. Pages (legacy /
# deploy-from-branch) auto-rebuilds on push to main — no deploy step needed.

on:
  schedule:
    - cron: '20 * * * *'   # hourly, offset 20 min after CC's top-of-hour fetch
  workflow_dispatch:        # manual trigger from the Actions tab

permissions:
  contents: write           # push refreshed snapshots
  issues: write             # open an issue if a run fails

# Never let a manual run and the cron run push concurrently.
concurrency:
  group: refresh-data
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Refresh snapshots (flight-data.js + progress-data.js)
        run: node scripts/refresh_snapshots.mjs

      - name: Commit & push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add flight-data.js progress-data.js ngt-data.js
          if git diff --cached --quiet; then
            echo "No snapshot changes — nothing to commit."
          else
            git commit -m "chore: refresh data snapshots $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            # Push with retry. On a non-fast-forward rejection, replay our commit
            # on top of the new remote tip, keeping OUR freshly written snapshots
            # (`-X theirs` auto-resolves the conflict on the regenerated files).
            for attempt in 1 2 3 4 5; do
              if git push origin main; then
                echo "Pushed on attempt $attempt"; break
              fi
              if [ "$attempt" = 5 ]; then
                echo "::error::push failed after 5 attempts"; exit 1
              fi
              echo "Push rejected — syncing with remote (attempt $attempt)…"
              git fetch origin main
              git rebase -X theirs origin/main || { git rebase --abort; exit 1; }
              sleep $((attempt * 3))
            done
          fi

      - name: Report failure as an issue
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title = `[Refresh] Data snapshot run failed – ${new Date().toISOString().slice(0,16)} UTC`;
            const { data: open } = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo,
              state: 'open', labels: 'refresh-failure',
            });
            if (open.length === 0) {
              await github.rest.issues.create({
                owner: context.repo.owner, repo: context.repo.repo,
                title,
                body: [
                  `Workflow run failed: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
                  '',
                  'The bundled snapshots may be stale. Likely causes:',
                  '- Command Center `flight-data.js` raw URL changed or that repo went private',
                  '- the `ap127-data-api` worker is down or changed its response shape',
                  '',
                  'Re-run manually from the Actions tab once the upstream is healthy.',
                ].join('\n'),
                labels: ['refresh-failure'],
              });
            }

      # Auto-close a stale refresh-failure issue once a run succeeds again.
      #
      # The step above only opens a NEW issue when none is already open, so an
      # issue nobody ever closes silently swallows every future alert. CMD_CTR
      # and DB001 both learned this the hard way (CMD_CTR issue #8 sat open
      # 2026-07-18→08-25 and ate the alerts for a real 7.5 h portal outage) and
      # both grew an auto-close step; this workflow was the last one still
      # missing it. Added 2026-09-02.
      - name: Close stale refresh-failure issue on success
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo,
              state: 'open', labels: 'refresh-failure',
            });
            for (const issue of issues) {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: issue.number,
                body: `Resolved — run ${context.runId} refreshed snapshots successfully. Auto-closing.`,
              });
              await github.rest.issues.update({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: issue.number,
                state: 'closed',
              });
            }
